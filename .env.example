# Frontend dev server reads these via import.meta.env (if you wired Vite)
VITE_API_BASE=http://localhost:8000

# Backend session defaults (Phase-2/3 will use these)
TEXT_MODEL=gpt-4o-mini
REALTIME_MODEL=gpt-4o-realtime-preview
REALTIME_VOICE=verse
WS_BASE=wss://api.openai.com/v1/realtime

# For later phases (realtime proxy / text gen)
# OPENAI_API_KEY=sk-...

